import JoinClient from '../../components/pages/JoinClient'

export const metadata = {
  title: 'Join Free — ML Tennis Predictions & Picks Tracker',
  description: 'Create your free account to track your tennis picks, follow AI predictions and get daily match previews.',
  alternates: { canonical: 'https://ratethat.tennis/join' },
}

export default function JoinPage() {
  return <JoinClient />
}
